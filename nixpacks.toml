[variables]
    NIXPACKS_PHP_ROOT_DIR = '/app/public'

[phases.setup]
    nixPkgs = ['...', 'php82-fpm', 'nginx'] # Gardez vos paquets par défaut

[start]
    cmd = "nginx -g 'daemon off;' & php-fpm"
